{%- set classes = [
  'cta-group',
  expand_to_fit ? 'cta-group--expand-to-fit',
  align_right ? 'cta-group--align-right',
]|filter(class => class is not empty) -%}
<div class="{{- classes|join(' ') -}}">
  {%- for cta in ctas -%}
    {{- cta -}}
  {%- endfor -%}
</div>
