{% set classes = ['drop-button'] %}
{% if align in ['left', 'center', 'right'] %}
  {% set classes = classes|merge(['drop-button--align-' ~ align]) %}
{% endif %}
<div class="{{ classes|join(' ' ) }}">
  <div role="button" class="drop-button__toggle" tabindex="0" aria-expanded="false">
    {{ toggle_content }}
  </div>
  {% set content_classes = [
    'drop-button__content',
    width in ['stretch', 'wide'] ? 'drop-button__content--' ~ width,
  ]|filter(v => v is not empty) %}
  <div class="{{ content_classes|join(' ') }}" tabindex="0">
    {{ panel_content }}
  </div>
</div>
